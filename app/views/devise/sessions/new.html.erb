<h2>Log in</h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div><%= f.label :identity_url %><br />
  <%= f.text_field :identity_url, autofocus: true %></div>

  <div><%= f.submit "Log in" %></div>
<% end %>

<%= render "devise/shared/links" %>
