<% if can? :decline_event_suggestion, event %>
	<%= link_to t('.decline', :default => t("helpers.links.decline")), decline_event_suggestion_path(event), :class => 'btn btn-danger btn-xs' %>
<% end %> 
<% if can? :approve_event_suggestion, event %>   
    <%= link_to t('.approve', :default => t("helpers.links.approve")), approve_event_suggestion_path(event), :class => 'btn btn-success btn-xs' %>
<% end %>
	<%= link_to t('.edit_original_event', :default => t("events.links.edit_original_event")), edit_event_path(event.event), :class => 'btn btn-warning btn-xs' %>