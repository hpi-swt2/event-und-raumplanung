<%- model_class = Group -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.edit', 'Edit %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>

<h3><%= User.model_name.human.pluralize %></h3>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= User.human_attribute_name(:identity_url) %></th>
      <th><%=t '.status', :default => t("helpers.status") %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.identity_url %></td>
        <td>
          <% if @group.users.include? user %>
            <%= 'zugewiesen' %>
          <% else %>
            <%= 'entfernt' %>
          <% end %>
        </td>
        <td>
          <% if @group.users.include? user %>
            <%= link_to 'entfernen', unassign_user_group_path(@group, user) %>
          <% else %>
            <%= link_to 'zuweisen', assign_user_group_path(@group, user) %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= render :partial => 'form' %>
